import * as React from 'react';

interface state {}

interface props {
    steam_id: number;
}

export class ProfileSettings extends React.Component<props, state> {
    componentDidMount() {}

    render() {
        return <></>;
    }
}
