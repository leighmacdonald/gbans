layout {
    pane split_direction="horizontal" {
        pane command="just" size="16%" {
            args "db"
        }

        pane command="just" size="16%" {
            args "demostats_serve"
        }

        pane command="just" {
            args "serve"
        }

        pane command="just" {
            args "air"
        }
    }
    // pane size=1 borderless=true {
    //     plugin location="zellij:compact-bar"
    // }
}
