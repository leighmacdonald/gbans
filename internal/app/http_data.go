package app

// FIXME this is currently disabled
// This is in its own file so that it can easily be skipped when linting since the
// frontend files are not present in the dist directory yet which causes a typecheck linter error
// go:embed dist
// var content embed.FS
